import FilteredCSS from '../css/FilteredCSS.css'
import Searchbar from '../components/Searchbar';

const FilteredResults = (props) => {
    return (
        <Searchbar />
    )
}

export default FilteredResults
